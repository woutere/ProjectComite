@model IEnumerable<ProjectComite.Models.Gemeente>

@{
    ViewData["Title"] = "Index gemeenten";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.postcode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.naam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.postcode)
            </td>
            <td>
                @if (this.User.IsInRole("Admin"))
                {
                    <a asp-action="Edit" asp-route-id="@item.gemeenteId">Edit</a>
                }
                <a asp-action="Details" asp-route-id="@item.gemeenteId">Details</a> 
                @if (this.User.IsInRole("Admin"))
                {

                    <a asp-action="Delete" asp-route-id="@item.gemeenteId">Delete</a>
                }
            </td>
        </tr>
}
    </tbody>
</table>
