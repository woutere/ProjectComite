@model ProjectComite.ViewModels.CreateGemeenteViewModel
@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Gemeente</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="gemeente.naam" class="control-label"></label>
                <input asp-for="gemeente.naam" class="form-control" />
                <span asp-validation-for="gemeente.naam" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="gemeente.postcode" class="control-label"></label>
                <input asp-for="gemeente.postcode" class="form-control" />
                <span asp-validation-for="gemeente.postcode" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                @{
                    for (int i = 0; i < Model.acties.Count(); i++)
                    {
                        <div>
                            <input asp-for="@Model.acties[i].CheckboxAnswer" type="checkbox" />
                            <label asp-for="@Model.acties[i].actieId">@Model.acties[i].naam</label>
                            <input asp-for="@Model.acties[i].actieId" type="hidden" />
                            <input asp-for="@Model.acties[i].naam" type="hidden" />
                        </div>
                    }
                }
            </div>
            <div class="form-group">
                @{
                    for (int i = 0; i < Model.leden.Count(); i++)
                    {
                        <div>
                            <input asp-for="@Model.leden[i].CheckboxAnswer" type="checkbox" />
                            <label asp-for="@Model.leden[i].lidId">@Model.leden[i].naam</label>
                            <input asp-for="@Model.leden[i].lidId" type="hidden" />
                            <input asp-for="@Model.leden[i].naam" type="hidden" />
                        </div>
                    }
                }
            </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
